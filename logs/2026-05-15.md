# 2026-05-15

## Pose-Based Scene Fix After Screenshot

- User tested the pose-based `sim.moveToPose` patch and reported:
  - cardboard still picked in the air;
  - cardboard is held vertically;
  - cardboard is released in the air instead of on `/Pallet_bottles`;
  - loaded pallet exits in the opposite direction.
- Updated `scripts/coppeliasim/lua/final_scene_palletizing_cycle.lua`:
  - moved `cycleIkTip` from gripper origin to lower local TCP offset `{0, -0.105, 0}`;
  - changed target TCP pose calculation to use world vertical `Z` height, not payload-local `Z`;
  - changed `attachLoad()` to keep the payload world pose/orientation on attachment, fixing the vertical cardboard artifact;
  - restored final release correction so payloads are set to the planned stack pose while logging large correction distances;
  - reversed pallet outfeed direction from `Y - 1.25` to `Y + 1.25`;
  - added `poseReachTolerance` warning logs when TCP remains far from the requested target.
- Installed the updated script into `/base_respondable/palletizing_cycle_script`.
- ZMQ smoke test after the patch:
  - first cardboard now gets corrected to the planned pallet stack pose after release;
  - the script continued into water-bundle phases without Lua compile failure.
- Important diagnostic:
  - sampled `motor1..motor3` reachable workspace in stopped scene;
  - original `/Cartoon` pickup target is about `0.925 m` from the nearest sampled TCP point;
  - low `/Pallet_bottles` contact target is about `1.281 m` from the nearest sampled TCP point;
  - therefore the remaining "pickup in the air" cannot be fully solved with Lua IK alone while keeping the exact current object coordinates.
- Next decision: either move the object/pallet/robot cell into reachable workspace, keep a hybrid snap-corrected demo, or simplify the scene geometry for a reliable full-cycle RPZ evidence run.

## Closed-Chain IK Correction After User Note

- User pointed out that the previous reachability sweep was invalid because direct joint setting ignored dummy/parallel-link closure; effectively `motor3` drifted in the air.
- Corrected the conclusion in [[01_CURRENT_STATE]], [[03_TASKS]], and [[final_scene_palletizing_cycle]].
- Updated `scripts/coppeliasim/lua/final_scene_palletizing_cycle.lua`:
  - added a custom closed-chain `simIK` environment/group;
  - added loop-closure elements for `dummy1A/B`, `dummy2A/B`, `dummy3A/B`, and `dummy4A/B`;
  - configured all IK group joints, not only `motor1..motor3`;
  - replaced simple `sim.moveToPose` as the preferred movement method;
  - removed direct `cfgTransfer` during payload transfer;
  - kept old `Above/Down` configs only as rough seeds before closed-chain IK refinement.
- Installed the updated script into `/base_respondable/palletizing_cycle_script`.
- Smoke-test observations:
  - dummy loop errors stayed essentially zero, so the mechanism no longer visibly breaks like in the raw sweep;
  - phase reached `grip_contact` for cardboard;
  - TCP still remained short in `Y`: `cycleIkTip` about `[-1.681, 0.129, 0.205]`, cardboard about `[-1.653, 0.845, 0.149]`.
- Next tuning should happen only under the closed-chain IK model: adjust target point on cardboard, seed configs, TCP offset, or source/stack geometry.

## Cardboard Y-Stall Follow-Up

- User reported that the robot appears to freeze above the cardboard and does not continue moving along `Y`.
- Updated `scripts/coppeliasim/lua/final_scene_palletizing_cycle.lua`:
  - added `cardboardGripPose`, a separate pickup target near the robot-facing cardboard edge;
  - kept `/Cartoon` generation at the original template pose;
  - reduced closed-chain IK per-target timeout from `8.0 s` to `3.0 s` so the cycle does not visually hang too long in a local minimum.
- Tested a temporary `tcpLocalOffset = {0, -0.105, -0.28}` to move the TCP forward, but it shifted the TCP mainly in the wrong world direction for the cardboard pose; reverted to `{0, -0.105, 0}`.
- Tested enabling scene dummy links as `dummy_linktype_gcs_loop_closure`; this made loop errors grow to meters, so the change was reverted and all dummy link types were reset to `0`.
- Latest smoke test with the reverted link types:
  - loop errors stayed near zero;
  - phase reached `grip_contact` and `lift_with_load`;
  - TCP still stayed at about `Y=0.166`, while the cardboard remained at about `Y=0.835`.
- Current conclusion: the closed-chain IK is no longer breaking the mechanism, but the current pickup target/seed still leaves the gripper short in `Y`. Next realistic fix is to tune the pickup fixture/geometry or use a reachable pickup pose for the generated cardboard.

## `simpleManipulatorPathPlanning` Inspection

- User asked to continue with frequent references to the knowledge base and to consider the CoppeliaSim example `simpleManipulatorPathPlanning`.
- Loaded and inspected `C:\Program Files\CoppeliaRobotics\CoppeliaSimEdu\scenes\pathPlanning\simpleManipulatorPathPlanning.ttt`, then restored the working scene from `scratch\current_scene_before_simpleManipulatorPathPlanning_lookup.ttt` and reinstalled the canonical Lua script.
- Extracted the relevant example pattern:
  - `simIK.findConfigs` searches for one or more valid joint configurations for the target tip pose;
  - `simOMPL` plans a path from the current configuration to the selected goal configuration;
  - local IK is then used only for the short approach/withdraw motion.
- Implemented an experimental closed-chain goal-config helper in `scripts\coppeliasim\lua\final_scene_palletizing_cycle.lua`, but disabled its live use after smoke testing:
  - direct/manual application of sampled configs can violate the imported robot dummy-loop closure;
  - worst smoke-test loop error observed in the risky variant was about `0.77 m` on `dummy3`;
  - after rollback to `syncWorlds=true`, loop errors returned to near zero.
- Retained one safe source change from the experiment:
  - exact `pick` and `place` down motions no longer jump through old `cfg*Down` seeds before pose IK, because those seeds were pulling the TCP away from the approach pose.
- Latest stable smoke test after rollback:
  - phases reached: `ready`, `pallet_arrived`, `cardboard_generated`, `lift_before_pick`, `pick`, `grip_contact`;
  - `cycleIkTip` at grip contact about `[-1.660, 0.687, 0.563]`;
  - cardboard about `[-1.653, 0.845, 0.149]`;
  - dummy-loop errors near zero (`dummy1..dummy4` all around `1e-5..4e-5 m`).
- Current conclusion:
  - `simpleManipulatorPathPlanning` is useful as the next architectural direction, but a direct OMPL copy is unsafe for this closed-chain imported model;
  - next practical choice is either a separate validated planning helper that checks dummy-loop error along candidate paths, or a small scene-geometry/pickup-fixture adjustment to put `/Cartoon` in a reachable contact zone.

## Robot Motion / Teleport Diagnosis

- User noted that the remaining visible problem is robot motion: objects still teleport, although they now land in the correct final positions.
- Confirmed that the visible teleport is mainly caused by `releaseLoad()` correction: the script snaps the carried object to the planned stack pose because the TCP did not physically reach the pickup/place pose.
- Tested whether the low cardboard contact point is blocked by joint limits:
  - current joint limits: low cardboard pickup target and approach target were not found by `simIK.findConfigs`;
  - temporary expanded limits `motor2 ~= [-90, 130] deg`, `motor3 ~= [-80, 170] deg` allowed `findConfigs` to find the low target.
- Rejected the temporary joint-limit expansion as a live fix:
  - applying the found config reached the target with about `0.0003 m` TCP error;
  - but dummy-loop errors became invalid (`dummy1/dummy2` about `0.05 m`, `dummy4` about `0.16 m`);
  - a live smoke test with expanded limits made motion worse and moved the cardboard physically.
- Restored the open scene from `scratch\current_scene_before_simpleManipulatorPathPlanning_lookup.ttt` and reinstalled the canonical Lua source.
- Current conclusion:
  - the scene has correct object final poses and stable loop closure in the safe mode;
  - removing visible teleport requires changing the reachable pickup/place geometry or building a validated planner that enforces dummy-loop closure along the path;
  - simply widening joint ranges or directly applying sampled IK configs is not acceptable for the imported closed-chain model.

## Release-Correction Smoothing

- Ran a stopped-scene closed-chain reachability scan with candidate configs validated by actual scene dummy-loop errors.
- Findings:
  - current cardboard pickup tool point is not valid;
  - current water pickup tool point is not valid;
  - current pallet/cardboard and pallet/water place tool points are not valid;
  - one valid cardboard-like pickup point was found near `X=-1.30`, `Y=-0.20`, `TCP Z=0.55` with max loop error about `0.001 m`;
  - one low place-like point was found near `X=-1.30`, `Y=-0.60`, `TCP Z=0.335`;
  - current geometry therefore cannot fully remove correction snaps without moving the cell geometry or adding a stronger validated planner.
- Updated `scripts\coppeliasim\lua\final_scene_palletizing_cycle.lua`:
  - added `releaseCorrectionSpeed`, `releaseCorrectionMinDuration`, and `releaseCorrectionMaxDuration`;
  - changed `releaseLoad()` so large release corrections detach the payload to world and move it linearly to the planned stack pose over a short duration instead of instant `sim.setObjectPose`;
  - kept the final pose correction, so objects still land at the correct planned coordinates.
- Smoke test after the patch:
  - phases reached `lift_after_place` for first cardboard without Lua failure;
  - cardboard ended at the planned pallet position;
  - TCP still did not physically reach the pallet point, so this is a visual smoothing/defensive correction, not a solved robot reachability model.

## CoppeliaSim Manual And Example Analysis

- User asked to analyze the local manual and examples that could solve the robot-motion problem.
- Read the project memory again: [[00_INDEX]], [[01_CURRENT_STATE]], [[03_TASKS]], and [[docs/vkr_practice/pak_pipeline_plan]].
- Inspected local CoppeliaSim manual pages:
  - `manual\en\solvingIkAndFk.htm`;
  - `manual\en\pathAndMotionPlanningModules.htm`;
  - `manual\en\simIK.htm`;
  - `manual\en\simOMPL.htm`.
- Loaded local example scenes through ZMQ, extracted their script text into `scratch\coppeliasim_example_scripts`, then restored `scratch\current_scene_before_simpleManipulatorPathPlanning_lookup.ttt` and reinstalled the canonical Lua source without saving the scene.
- Most relevant extracted examples:
  - `kinematics_parallel_7__01__Script.lua`: official parallel-mechanism pattern with main IK group plus closure-only fallback group;
  - `kinematics_random_8__01__IRB4600_Script.lua`: `simIK.findConfigs` with validation callback;
  - `kinematics_debug_10__01__IRB4600_Script.lua`: `simIK.handleGroup(..., {debug=1|2})`;
  - `kinematics_ikPathGeneration__01__UR10_Script.lua`: `simIK.generatePath`;
  - `path_simpleManipulatorPathPlanning__01__UR5_Script.lua`: serial-manipulator architecture `findConfigs -> OMPL path -> short IK approach`;
  - `path_stateValidationCallback_lua__01__StartConfiguration_Script.lua`: save/apply/check/restore validation callback pattern.
- Created [[docs/vkr_practice/coppeliasim_motion_manual_analysis]].
- Updated [[00_INDEX]], [[01_CURRENT_STATE]], [[02_DECISIONS]], and [[03_TASKS]].
- Current conclusion: do not copy OMPL directly; first add an example-7-style closure-only fallback IK group and optional debug overlay, then decide whether geometry must be shifted into the already identified valid workspace.

## Closure Fallback And Layer-1 Smoke Test

- Continued from [[docs/vkr_practice/coppeliasim_motion_manual_analysis]] and implemented the diagnostic plan in `scripts\coppeliasim\lua\final_scene_palletizing_cycle.lua`.
- Added:
  - `closedIkGroupFallback`, a closure-only IK group following CoppeliaSim example `7-fkAndIkResolutionForParallelMechanisms.ttt`;
  - optional debug overlay signal `palletizing_debug_ik`;
  - `palletizing_last_ik_warning` string signal;
  - explicit `target_unreachable` warnings with TCP error, loop error, and fallback status.
- Installed the script into the open scene through `scripts\coppeliasim\python\install_palletizing_lua.py`.
- First diagnostic smoke test:
  - reached first cardboard `lift_after_place`;
  - loop error stayed `0.000 m`;
  - TCP errors confirmed targets were unreachable rather than the mechanism breaking.
- User reported moving pallets closer; measured the open scene:
  - `/Cartoon` approximately `[-1.6877, 0.4528, 0.5199]`;
  - `/Pallet_cartoon` approximately `[-1.6877, 0.4528, 0.443]`;
  - `/Pallet_bottles` approximately `[0.6964, -1.5475, 0.134]`.
- Tested moving `/Pallet_bottles`, `/Pallet_cartoon`, and `/Cartoon` into the old reachability-scan region; this worsened pickup/place errors, so the test was reverted.
- Restored `/Pallet_bottles` height in the open scene to about `Z=0.247`; this modestly improved cardboard place error but did not remove TCP mismatch.
- Verified `tcpLocalOffset = {0, -0.105, 0}` is physically reasonable for the gripper bounding box, so the remaining height error is not caused by an obviously wrong TCP local offset.
- Reduced `closedIkTargetTimeout` from `1.2 s` to `0.6 s`.
- Layer-1 smoke test with the faster failover:
  - completed `cardboard + water_bundle_1 + water_bundle_2 + water_bundle_3`;
  - reached `return_home_between_layers` for layer 1;
  - no `cycle_aborted`;
  - warnings still show nonzero TCP errors, but `loop error = 0.000 m` throughout.
- Current conclusion:
  - the script is now better suited for a full PAK telemetry run because it progresses through unreachable poses;
  - visual robot reach remains imperfect, but the closed-chain mechanism stays valid and scripted release correction keeps object placement logical.

## uArm Reference And Scene Layout Correction

- User corrected the direction of work: do not solve the diploma robot blocker by moving `/Pallet_bottles` or `/conveyor_bottles` away from the intended scene layout.
- Reverted test-only open-scene moves:
  - `/conveyor_bottles = [-0.45, -1.625, 0.45]`;
  - `/Pallet_bottles = [0.6964, -1.5475, 0.247]`.
- Extracted in-scene uArm scripts:
  - `scratch\uarm_robot_script.lua`;
  - `scratch\uarm_gripper_script.lua`.
- Finding:
  - `/uarm/Script` moves by calibrated `sim.moveToConfig` joint positions;
  - `/uarm/uarmVacuumGripper/Script` performs physical suction by checking respondable shapes with a proximity sensor and connecting dummy links via `sim.setLinkDummy`;
  - this is a better reference for the diploma robot than moving scene geometry.
- Current next direction:
  - keep the intended pallet/conveyor layout;
  - adapt the uArm-style separated gripper script/contact logic to `/base_respondable`;
  - avoid long-distance payload correction as the main placement mechanism.

## uArm-Style Config Motion Implemented

- Updated `scripts\coppeliasim\lua\final_scene_palletizing_cycle.lua`.
- Added `useUarmStyleConfigMotion = true`; the default live cycle now uses calibrated `sim.moveToConfig` movement like `/uarm/Script`.
- Added `palletizing_calibration_mode` signal so the open scene can be started without running the palletizing cycle while calibrating motor configs.
- Kept the intended scene layout:
  - `/conveyor_bottles = [-0.45, -1.625, 0.45]`;
  - `/Pallet_bottles = [0.6964, -1.5475, 0.247]`.
- Calibrated and added:
  - `cfgCardboardPlaceAbove` / `cfgCardboardPlaceDown` per layer;
  - `cfgWaterPlaceAboveByLayer` / `cfgWaterPlaceDownByLayer` per layer and bundle row.
- Kept `allowLargeReleaseCorrection = false`, so large hidden teleport-style release corrections still fail the cycle instead of hiding a bad config.
- Verification:
  - installed source with `scripts\coppeliasim\python\install_palletizing_lua.py`;
  - first smoke test completed layer 1;
  - second smoke test completed layer 2;
  - full smoke test reached `cycle_complete` at about `256 s` simulation time;
  - completed 4 cardboard sheets and 12 water bundles;
  - no `release blocked` warning in the full run;
  - loaded pallet moved out on `/conveyor_pallet`.
- The canonical scene file `scenes\final_scena_diplom.ttt` was not saved; user should visually verify the open scene before saving.
