# 2026-05-14

## VKR Practical Data Pipeline

- Continued from the knowledge base after likely context compaction.
- Read:
  - [[00_INDEX]]
  - [[01_CURRENT_STATE]]
  - [[03_TASKS]]
  - [[docs/vkr_practice/pak_pipeline_plan]]
- Implemented file-based VKR PAK data pipeline under `scripts/data_pipeline/`:
  - `pipeline_common.py`
  - `normalize_telemetry.py`
  - `validate_telemetry.py`
  - `build_features.py`
  - `simulate_degradation.py`
  - `estimate_rul.py`
  - `make_vkr_figures.py`
  - `run_file_pipeline.py`
- Created/used output folders:
  - `data/telemetry/vkr_normalized/`
  - `data/telemetry/vkr_validated/`
  - `data/features/`
  - `data/experiments/`
  - `data/results/`
  - `reports/figures/vkr_practice/`
- Smoke-tested the pipeline on legacy CSV files:
  - `data/telemetry/test2_dynamics_monitor.csv`
  - `data/telemetry/test2_joint_torques.csv`
- Smoke-test outputs:
  - normalized rows: `2243`
  - valid rows: `2243`
  - invalid rows: `0`
  - `K_data = 1.0`
  - `K_phase = 1.0`
  - base feature rows: `4`
  - degradation scenario rows: `1280`
  - RUL estimate rows: `1280`
  - metric rows: `16`
- Generated SVG figures:
  - `reports/figures/vkr_practice/torque_rms_by_axis.svg`
  - `reports/figures/vkr_practice/hi_curves_motor1.svg`
  - `reports/figures/vkr_practice/rul_actual_predicted_s3_motor1.svg`
  - `reports/figures/vkr_practice/pak_dashboard_summary.svg`
- Added [[docs/vkr_practice/data_pipeline_state]].
- Updated [[docs/vkr_practice/pak_pipeline_plan]], [[00_INDEX]], [[01_CURRENT_STATE]], and [[03_TASKS]].

## Important Limitation

- The smoke test uses old `test2` telemetry and the synthetic phase label `legacy_unsegmented`.
- Final VKR evidence still requires phase-linked telemetry from `scenes/final_scena_diplom.ttt` using `customData.palletizingCycle`.

## Next

1. Collect final scene telemetry into `data/telemetry/vkr_raw/`.
2. Re-run the file pipeline on final scene telemetry.
3. Then return to cardboard pickup and full palletizing-cycle stabilization.

## Final Scene Live Collector

- Added `scripts/coppeliasim/python/collect_final_scene_telemetry.py`.
- The collector reads:
  - `/base_respondable/motor1` ... `/base_respondable/motor4`;
  - `/base_respondable` custom data property `customData.palletizingCycle`.
- Output is JSONL compatible with the existing `normalize_telemetry.py` stage.
- Use system `python` for this script because it already has `zmq` and can import CoppeliaSim ZMQ Remote API from `C:\Program Files\CoppeliaRobotics\CoppeliaSimEdu\programming\zmqRemoteApi\clients\python\src`.
- Verified:
  - system Python imports `zmq`;
  - system Python imports `coppeliasim_zmqremoteapi_client`;
  - `python .\scripts\coppeliasim\python\collect_final_scene_telemetry.py --help` works.

## First Live Final-Scene Capture

- Captured raw telemetry from CoppeliaSim into `data/telemetry/vkr_raw/final_scene_live_01.jsonl`.
- Processed the live JSONL through the file pipeline.
- Initial validation showed low `K_data` because JSON normalization used `or` and treated numeric zero values as empty.
- Fixed `scripts/data_pipeline/normalize_telemetry.py` to preserve `0.0` values in `time`, `q`, `omega`, `accel`, and `torque`.
- Re-ran the file pipeline successfully:
  - normalized rows: `5368`
  - valid rows: `5368`
  - invalid rows: `0`
  - `K_data = 1.0`
  - `K_phase = 1.0`
  - phase count: `9`
  - feature rows: `36`
  - degradation scenario rows: `11520`
  - RUL estimate rows: `11520`
- Captured phases include `pallet_arrived`, `cardboard_generated`, `lift_before_pick`, `pick`, `grip_contact`, `lift_with_load`, `move_to_pallet`, `place`, and `water_bundle_generated`.
- Limitation: first live capture is partial and stopped around simulation time `26.85 s`; it did not reach `cycle_complete`.

## InfluxDB and Grafana Scaffold

- Updated `scripts/coppeliasim/python/collect_final_scene_telemetry.py`:
  - waits for CoppeliaSim simulation start by default;
  - supports `--stop-on-phase cycle_complete`;
  - avoids writing stale stopped-scene data before Play.
- Added helper `scripts/coppeliasim/python/run_final_scene_full_collection.ps1`.
- Added InfluxDB/Grafana stack under `infra/pak/`:
  - `docker-compose.yml`;
  - Grafana datasource provisioning;
  - Grafana dashboard provisioning;
  - starter dashboard `vkr_pak_dashboard.json`.
- Added `scripts/data_pipeline/export_to_influx.py`.
- Verified exporter with `--dry-run`: generated `17020` line-protocol rows from current telemetry, features, and RUL outputs.
- Verified local Docker availability:
  - Docker version `29.1.2`;
  - Docker Compose version `v2.40.3-desktop.1`;
  - `docker compose config` is valid for `infra/pak`.

## Full Capture Processing and InfluxDB Export

- User ran full collection and Docker/Grafana.
- Found `data/telemetry/vkr_raw/final_scene_full_01.jsonl`.
- The file pipeline was still pointing to previous `final_scene_live_01.jsonl`, which is why export stayed at `17020` lines.
- Re-ran pipeline on `final_scene_full_01.jsonl`:
  - normalized rows: `4608`
  - valid rows: `4608`
  - `K_data = 1.0`
  - `K_phase = 1.0`
  - phase count: `13`
  - feature rows: `52`
  - degradation scenario rows: `16640`
  - RUL estimate rows: `16640`
- `cycle_complete` was not found in the raw file.
- Final captured phases include `return_home` and `pallet_outfeed`, but the end of the file contains `unknown`.
- Updated `scripts/data_pipeline/export_to_influx.py`:
  - default timestamp mode is now `align-end`;
  - completed simulations are visible in Grafana immediately instead of being partly timestamped in the future.
- Added `scripts/data_pipeline/run_pipeline_and_export.ps1`.
- Added optional `-InfluxLive` mode to `scripts/coppeliasim/python/run_final_scene_full_collection.ps1`.
  - Live mode writes `vkr_motor_telemetry` and `vkr_cycle_state` to InfluxDB during simulation.
  - Features and HI/RUL remain post-run batch exports.
- Exported current artifacts to InfluxDB:
  - `influx_lines_written=22452`.
- Verified via Flux query that `final_scene_full_01` exists in InfluxDB measurements:
  - `vkr_cycle_state`;
  - `vkr_motor_telemetry`;
  - `vkr_phase_features`;
  - `vkr_rul_estimates`.

## Second Full Capture Processing

- Found `data/telemetry/vkr_raw/final_scene_full_02.jsonl`.
- `cycle_complete` was not found.
- Tail of the file shows repeated samples at simulation time about `181.35 s`.
- Final captured phase: `place`, layer `4`, item `water_bundle_1`, `carrying=true`.
- Fixed `scripts/data_pipeline/run_pipeline_and_export.ps1` parameter name from `Input` to `InputPath` because `$Input` conflicts with a PowerShell automatic variable.
- Re-ran pipeline and export:
  - normalized rows: `7164`
  - valid rows: `7164`
  - `K_data = 1.0`
  - `K_phase = 1.0`
  - phase count: `10`
  - feature rows: `40`
  - degradation scenario rows: `12800`
  - RUL estimate rows: `12800`
  - `influx_lines_written=20987`
- Next work should focus on CoppeliaSim scene stabilization, especially final-layer `place` behavior.

## Neural Network RUL Layer

- User asked to use `scikit-learn`; the previous manual NumPy MLP was replaced.
- Rolled back the last attempted log-derived features (`log_remaining_alpha`, `neg_log_degradation_rate`).
- Added `scripts/data_pipeline/train_rul_mlp.py` based on `sklearn.neural_network.MLPRegressor`.
- The model learns `log1p(RUL_actual)` from:
  - phase/axis/scenario categorical features;
  - diagnostic feature table values;
  - derived degradation features `degradation_progress`, `degradation_rate`, `remaining_alpha`, `HI`, `is_no_degradation`.
- Updated `scripts/data_pipeline/run_file_pipeline.py` to call `train_rul_mlp.py`.
- Updated `scripts/data_pipeline/export_to_influx.py` to export `vkr_nn_rul_predictions`.
- Updated `scripts/data_pipeline/run_pipeline_and_export.ps1` to use system `python`, because bundled Python does not have `scikit-learn`.
- Current run on `final_scene_full_02`:
  - neural-network prediction rows: `12800`
  - preliminary metrics and export count from the first scikit-learn pass were superseded by the later correction below.

## Neural Network Pipeline Correction

- Continued after context compaction using [[00_INDEX]], [[01_CURRENT_STATE]], [[03_TASKS]], [[docs/vkr_practice/pak_pipeline_plan]], and [[docs/vkr_practice/data_pipeline_state]].
- Stabilized the scikit-learn neural-network stage:
  - `scripts/data_pipeline/train_rul_mlp.py` uses `sklearn.neural_network.MLPRegressor`;
  - default max iterations are now `2000`;
  - latest training converged in `166` iterations.
- Re-ran/verified the current `final_scene_full_02` artifact set:
  - prediction rows: `12800`;
  - metric rows: `32`;
  - average test `MAE = 2.5675`;
  - average test `RMSE = 3.0033`;
  - average test `R2 = 0.9662`.
- Updated the pipeline around the neural-network stage:
  - `scripts/data_pipeline/run_file_pipeline.py` runs `train_rul_mlp.py` before figure generation;
  - `scripts/data_pipeline/make_vkr_figures.py` produces `rul_nn_actual_predicted_s3_motor1.svg`;
  - `scripts/data_pipeline/export_to_influx.py` exports `vkr_nn_rul_predictions`, `vkr_rul_metrics`, and `vkr_nn_rul_metrics`;
  - `scripts/data_pipeline/run_pipeline_and_export.ps1` now defaults to `final_scene_full_02`;
  - `infra/pak/grafana/dashboards/vkr_pak_dashboard.json` includes panels for neural-network RUL, absolute error, test MAE, and test R2.
- Verified exporter:
  - dry-run lines: `33835`;
  - real InfluxDB write: `influx_lines_written=33835`.
- Verified InfluxDB measurements now include:
  - `vkr_cycle_state`;
  - `vkr_motor_telemetry`;
  - `vkr_phase_features`;
  - `vkr_rul_estimates`;
  - `vkr_nn_rul_predictions`;
  - `vkr_rul_metrics`;
  - `vkr_nn_rul_metrics`.
- Updated [[docs/vkr_practice/data_pipeline_state]], [[docs/vkr_practice/pak_pipeline_plan]], [[01_CURRENT_STATE]], [[03_TASKS]], and `infra/pak/README.md`.
- Remaining practical blocker is unchanged: `final_scene_full_02` still ends at `place`, layer `4`, `water_bundle_1`, without `cycle_complete`; next work should debug/stabilize the CoppeliaSim scene.

## Runtime Runbook And Simplified Launch

- User clarified the intended PAK behavior: real-time telemetry from CoppeliaSim, neural-network RUL, RUL in hours, and maintenance warning.
- Added [[docs/vkr_practice/pak_runtime_runbook]] explaining the defendable architecture:
  - real-time collection/display through CoppeliaSim, JSONL, InfluxDB, and Grafana;
  - periodic/post-run neural-network retraining on accumulated feature data;
  - operational prediction from the latest trained model;
  - RUL conversion from cycles to hours after full-cycle duration is measured.
- Added `requirements-pak.txt` for another-computer setup.
- Added `scripts/pak/run_pak_demo.ps1` as the simplified demo launcher:
  - starts Docker Compose unless `-NoDocker` is passed;
  - runs CoppeliaSim telemetry collection with optional live InfluxDB export;
  - runs post-run analytics and InfluxDB export.
- Updated [[00_INDEX]], [[01_CURRENT_STATE]], [[03_TASKS]], [[docs/vkr_practice/pak_pipeline_plan]], and `infra/pak/README.md`.

## Scene Tuning Patch For User-Observed Issues

- User reported 7 scene issues:
  - unrealistically large motor moments;
  - cardboard moving on pallet like on a conveyor;
  - cardboard placed perpendicular to the final pallet;
  - distant object pickup;
  - water bundles placed into each other and in the wrong direction;
  - initially standing water bundles ejected by collisions;
  - cycle stopping roughly around the last quarter.
- Updated `scripts/coppeliasim/lua/final_scene_palletizing_cycle.lua`:
  - reduced motion limits: lower `maxVel`, `maxAccel`, and `maxJerk`;
  - reduced payload masses: cardboard `0.35 kg`, water bundle `9.0 kg`, pallet `18.0 kg`;
  - added recursive shape handling for masses/static/respondable flags;
  - set payload/template respondability off for scripted kinematic placement;
  - hidden templates are now also made static/non-respondable to avoid collisions with generated copies;
  - added `moveObjectToGripContact()` before attachment to reduce visible distance at pickup;
  - release now snaps payload directly to the final stack pose and parents it to the pallet, avoiding slide-on-pallet behavior;
  - cardboard and water placement poses now use 90-degree yaw correction;
  - water bundles are laid along the pallet from far side to near side using X offsets instead of previous Y offsets;
  - `cycle_complete` is now written before cleanup and held for `2 s` so the collector can capture it.
- Added installer helper `scripts/coppeliasim/python/install_palletizing_lua.py`.
- Installed the updated Lua source into the currently open CoppeliaSim script `/base_respondable/palletizing_cycle_script` via ZMQ without auto-saving the scene.
- Updated [[final_scene_palletizing_cycle]], [[01_CURRENT_STATE]], and [[03_TASKS]].
- Still needed: visually test, then save `scenes/final_scena_diplom.ttt` if the pass looks correct, and capture a new full run.

## Scene Tuning Patch After User Screenshot

- Continued with references to [[final_scene_palletizing_cycle]], [[01_CURRENT_STATE]], and [[03_TASKS]].
- User reported current remaining problems:
  - bottles should be generated by the initially located `/packofbottle_respondable` model;
  - `Cartoon` should appear at its original coordinates and be picked there;
  - cardboard should be placed at the same `x`/`y` as `/Pallet_bottles`;
  - bottles should be rotated back 90 degrees and placed in three rows as in the screenshot;
  - some objects still visibly fly from the release point to the final pose.
- Queried open CoppeliaSim scene via ZMQ:
  - `/packofbottle_respondable` pose about `[0.1252, -1.5303, 0.6261]`;
  - `/Cartoon` pose about `[-1.6527, 0.8448, 0.1489]`;
  - `/Pallet_bottles` pose about `[1.1464, -1.5475, 0.2470]`;
  - world footprint: water bundle about `0.281 x 1.316 m`, pallet about `0.800 x 1.500 m`.
- Updated `scripts/coppeliasim/lua/final_scene_palletizing_cycle.lua`:
  - `waterPlaceYaw = 0.0`;
  - `stackCenter` now uses `Pallet_bottles` `x`/`y` directly;
  - `cardboardPickPose = templatePoses.cardboard`;
  - cardboard no longer moves from original pose to a separate pickup pose before robot pickup;
  - water bundle clones no longer run through conveyor movement during generation;
  - water row offsets are centered around the pallet with pitch based on bundle footprint, default about `0.28 m`;
  - water placement order is far-to-near across pallet width: `{+pitch, 0, -pitch}`;
  - removed the `moveObjectToGripContact()` call from the pickup sequence.
- Reinstalled the updated source into `/base_respondable/palletizing_cycle_script` via `scripts/coppeliasim/python/install_palletizing_lua.py`.
- Still needed: visual test in CoppeliaSim, then save the scene only if the layout is correct.

## Attach Dummy / Release Teleport Fix

- User identified the likely cause of distant pickup: `cycleAttachDummy` appears on simulation start with coordinates different from gripper/TCP and should match `motor4`.
- Queried open CoppeliaSim scene via ZMQ:
  - `/base_respondable/motor4` and `/base_respondable/gripper_respondable` have the same world position;
  - their orientations differ;
  - `cycleAttachDummy` was absent while simulation was stopped.
- Updated `scripts/coppeliasim/lua/final_scene_palletizing_cycle.lua`:
  - added `toolCenter = motors[4]`;
  - replaced object-pose dummy alignment with `alignAttachDummyToTool()`;
  - `cycleAttachDummy` is now parented to `/base_respondable/motor4`;
  - dummy local position and orientation are forced to `{0,0,0}`;
  - before each pickup, dummy is realigned to `motor4`, not to the payload;
  - added `releaseSnapTolerance = 0.12`;
  - release only snaps to the planned stack pose if the carried object is already close enough;
  - if the distance is larger, the script keeps the carried pose and logs a warning that place trajectory must be tuned.
- Reinstalled the updated source into `/base_respondable/palletizing_cycle_script`.
- Still needed: start simulation and verify in the scene tree that `cycleAttachDummy` has zero local pose under `motor4`; if placement warnings appear, the next fix is per-target robot placement configs/trajectory, not object teleportation.

## Attach Dummy Parent Revert

- User tested the `motor4` dummy parenting and reported it made pickup worse; requested keeping the same dummy coordinates but parenting it to `gripper`.
- Updated `scripts/coppeliasim/lua/final_scene_palletizing_cycle.lua`:
  - removed `toolCenter`;
  - `alignAttachDummyToTool()` now parents `cycleAttachDummy` to `/base_respondable/gripper_respondable`;
  - dummy world pose is aligned to gripper, then local position/orientation are forced to zero;
  - this alignment still runs on simulation start and before each attach.
- Reinstalled the updated source into `/base_respondable/palletizing_cycle_script`.
- Current expected check: on simulation start, `cycleAttachDummy` should be under `gripper_respondable` with local position and orientation close to zero.

## Manual Trajectory Table First Pass

- User chose the first proposed solution: manual calibrated joint configs, using the final object positions from before the last dummy-parenting change.
- Updated `scripts/coppeliasim/lua/final_scene_palletizing_cycle.lua`:
  - added `pickPlans` for cardboard and water pickup;
  - added `placePlans` for cardboard and three water rows;
  - water rows now use distinct first-pass `motor1` offsets: `+12 deg`, `0 deg`, `-12 deg`;
  - added small per-layer adjustment for motors 2/3;
  - added AABB gap check before attach with `attachGapTolerance = 0.10 m`;
  - if the gripper is too far from the object, attach is skipped and state becomes `pick_failed`;
  - failed pick aborts the cycle with `cycle_aborted` instead of silently grabbing at a distance.
- Reinstalled the updated Lua source into `/base_respondable/palletizing_cycle_script`.
- Added `scripts/coppeliasim/python/capture_robot_config.py`:
  - reads current `motor1..motor3` joint angles via ZMQ;
  - prints ready-to-paste Lua `rad(...)` config values;
  - useful after manually jogging the robot to exact pickup/placement poses.
- Quick capture of the current home-ish pose returned about `{-0.004, 0.033, -0.179}` degrees and gripper pose near `[-0.11007, -1.52198, 1.13856]`.
- Still needed: run visual test. If rows or pickup points are off, capture exact configs with `capture_robot_config.py` and replace the first-pass values in `pickPlans` / `placePlans`.

## Manual Trajectory Table Rollback

- User requested rollback.
- Reverted the last manual trajectory-table experiment:
  - removed `pickPlans` and `placePlans`;
  - removed AABB attach blocking and `pick_failed` / `cycle_aborted` logic from that experiment;
  - restored direct use of `cfgCardboardAbove`, `cfgCardboardDown`, `cfgBottleAbove`, `cfgBottleDown`, `cfgPalletAbove`, and `cfgPalletDown`;
  - deleted `scripts/coppeliasim/python/capture_robot_config.py`.
- Kept the earlier state:
  - `cycleAttachDummy` parented to `gripper_respondable` with zero local pose;
  - original `/Cartoon` and `/packofbottle_respondable` spawn poses;
  - cardboard centered on `/Pallet_bottles`;
  - water bundle orientation reset to the template orientation;
  - release snap tolerance remains `0.12 m`.
- Reinstalled the rolled-back Lua source into `/base_respondable/palletizing_cycle_script`.

## Pose-Based Gripper/TCP Motion Attempt

- User decided not to continue with per-object CFG calibration because it would take too long and asked to make the robot reach with the gripper to objects.
- Continued with references to [[final_scene_palletizing_cycle]], [[01_CURRENT_STATE]], and [[03_TASKS]].
- Inspected the old `scripts/coppeliasim/lua/Diploma_cellControllerScript.lua` pattern, which uses `sim.moveToPose` with `tip`, `target`, `base`, and `joints`.
- Updated `scripts/coppeliasim/lua/final_scene_palletizing_cycle.lua`:
  - added optional `simIK` loading and pose-based `sim.moveToPose`;
  - added hidden helper dummies `cycleIkTip` and `cycleIkTarget`;
  - parented `cycleIkTip` to `/base_respondable/gripper_respondable` with zero local pose;
  - kept `cycleAttachDummy` under `gripper_respondable`;
  - added object-height based conversion from payload pickup/final pose to gripper/TCP pose;
  - added `pickAndPlaceByPose()` and switched cardboard/water moves to it;
  - changed `attachLoad()` so the payload is held under the gripper with a top-center carry offset.
- Installed the updated source into the open CoppeliaSim script `/base_respondable/palletizing_cycle_script`.
- Ran a short ZMQ smoke test:
  - simulation started and stopped by script;
  - phases reached first-layer cardboard place and first-layer water bundle 1/2/3 place;
  - no Lua compile failure was observed.
- Still needed: visual verification in CoppeliaSim that the robot links and closure dummies do not diverge, and that the gripper visibly reaches the payloads before attachment.
