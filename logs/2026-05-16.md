# 2026-05-16

## CoppeliaSim Palletizing Cycle

- Continued from [[01_CURRENT_STATE]], [[03_TASKS]], and [[final_scene_palletizing_cycle]] after context compaction.
- Updated `scripts\coppeliasim\lua\final_scene_palletizing_cycle.lua`:
  - increased uArm-style config motion limits to target about 3 minutes per pallet;
  - changed cardboard placement yaw correction from `+90 deg` to `-90 deg` to avoid the visible 180-degree release flip;
  - lowered generated `cycle_loaded_pallet` to `Z=0.134` while leaving the source template `/Pallet_bottles` unchanged at `Z=0.247`;
  - added small limited attach-position correction under the gripper while preserving payload orientation;
  - added `cycle` to `customData.palletizingCycle`;
  - added default infinite cycle mode with signal override `palletizing_infinite_cycle`;
  - shortened grip/release/outfeed waits.
- Installed the source into the currently open CoppeliaSim scene via `scripts\coppeliasim\python\install_palletizing_lua.py`.
- Did not save `scenes\final_scena_diplom.ttt`.
- ZMQ full-cycle verification:
  - reached `cycle_complete`;
  - final `sim_time = 185.5 s`;
  - completed `4` cardboard sheets and `12` water bundles;
  - no `cycle_aborted`;
  - no final `palletizing_last_ik_warning`;
  - final outfeed pallet pose was approximately `[0.6964, -0.2975, 0.134]`.
- Next visual checks in CoppeliaSim:
  - cardboard should not spin at release;
  - payloads should look better under the gripper;
  - generated pallet should not hang above the pallet conveyor;
  - after outfeed, infinite mode should start a new pallet/cycle.

## Final Grip Visual Pass

- User reported the remaining issue: water bundle was held too far to the side of the gripper.
- Measured first water-bundle local position before the pass: about `{0.119, 0.212, 0.0}` relative to `/base_respondable/gripper_respondable`.
- Tested exact water centering `{0.0, 0.16, 0.0}`:
  - visual local position became centered;
  - release failed later because planned place configs were calibrated for the previous carry offset.
- Tested softer water offset `{0.04, 0.18, 0.0}`:
  - release still failed on layer 2 by a small margin.
- Final installed compromise:
  - `waterCarryLocalPosition = {0.06, 0.19, 0.0}`;
  - `releaseSnapTolerance = 0.20`;
  - cardboard carry correction remains disabled because exact cardboard centering caused a `0.307 m` release mismatch.
- Final ZMQ verification:
  - reached `cycle_complete`;
  - final `sim_time = 187.35 s`;
  - no `cycle_aborted`;
  - no final `palletizing_last_ik_warning`;
  - first water-bundle local carry pose after attach was approximately `{0.06, 0.19, 0.0}`.
- Source was installed into the open scene; `scenes\final_scena_diplom.ttt` was not saved.
- User visual feedback: water bundle still intersects/enters the gripper during carry, but the residual visual issue is accepted for now. Keep the stable full-cycle script and move on to scene saving/final telemetry unless gripper tuning is explicitly reopened.
- User saved the current stable scene as `scenes\pred_final.ttt`. Treat it as the pred-final scene for final telemetry and PAK evidence.

## Infinite Cycle Fix

- User noticed that after loaded pallet outfeed a new pallet did not appear.
- Cause: previous ZMQ tests left runtime signal `palletizing_infinite_cycle = 0` in the open scene.
- Cleared the signal in the open scene.
- Updated `shouldRunInfiniteCycle()`:
  - `nil`, `0`, and positive values mean repeat enabled;
  - only negative values disable repetition for deliberate single-cycle tests.
- Installed the updated source into the open scene.
- `scenes\pred_final.ttt` still needs to be saved again after visual confirmation that the second cycle starts.
- User confirmed visually that the infinite cycle now works correctly. Move on to final telemetry capture and PAK evidence generation.
